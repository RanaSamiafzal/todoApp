import React from 'react'; import { useDispatch, useSelector } from 'react-redux'; import { TodoItem, TodoListContainer, TodoText, DeleteButton, NoTodosMessage } from '../../components/StyledComponents'; import { deleteTodo, toggleTodo } from './todoSlice'; import { db } from '../../Config/fbConfig'; import { deleteDoc,doc,updateDoc } from 'firebase/firestore'; import { useAuth } from '../../context/AuthContext'; function TodoList() { const todos = useSelector((state)=> state.todos.todos ); const dispatch = useDispatch(); const { user } = useAuth(); const handleToggle = async (todo) => { await updateDoc(doc(db,"users", user.uid, "todos", todo.id), { completed : !todo.completed , }); dispatch(toggleTodo(todo.id)); }; const handleDelete = async (id) => { await deleteDoc(doc(db, "users", user.uid, "todos", id)); dispatch(deleteTodo(id)); }; console.log("Todos from Redux:", todos); return ( <TodoListContainer> {todos.length === 0 ? ( <NoTodosMessage>No Todos Yet</NoTodosMessage> ) : ( todos.map( (todo)=>  ( <TodoItem key={todo.id} completed={todo.completed}> <TodoText onClick={ ()=> handleToggle(todo) }> {todo.text}</TodoText> <DeleteButton onClick={()=> handleDelete(todo.id)}>Delete</DeleteButton> </TodoItem> ) ) ) } </TodoListContainer> ); } export default TodoList;
